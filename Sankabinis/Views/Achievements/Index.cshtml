@model IEnumerable<Sankabinis.Models.Achievement>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Pavadinimas)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Aprasas)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Pavadinimas)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Aprasas)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Pasiekimas">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Pasiekimas">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Pasiekimas">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
