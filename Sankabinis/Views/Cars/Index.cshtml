@model IEnumerable<Sankabinis.Models.Car>

@{
    ViewData["Title"] = "Index";
}

<h1>My Cars</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Marke)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Modelis)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Marke)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Modelis)
            </td>
            <td>

                <a asp-action="Details" asp-route-id="@item.Id_Automobilis">Details</a>
                
            </td>
        </tr>
}
    </tbody>
</table>
